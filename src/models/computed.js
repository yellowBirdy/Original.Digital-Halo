/**
 * Created by mpio on 03/11/16.
 */
/*import trackerCounts from '../../public/data/tracker_counts';
import demographicsPerTracker from '../../public/data/res_per_tracker_details';
import demographicsPerCompany from '../../public/data/res_per_company_details';*/



// TODO replace with local forage
const populateStorage = () => {

    console.log('executing populateStorage (now empty)');
    /*window.localStorage.setItem('trackerCounts', JSON.stringify(trackerCounts));
    window.localStorage.setItem('demoPerTracker', JSON.stringify(demographicsPerTracker));
    window.localStorage.setItem('demoPerCompany', JSON.stringify(demographicsPerCompany));*/

};

populateStorage();

export default populateStorage;